package unilim.info.ihm.tp6.exo3.controller;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.layout.HBox;
import javafx.scene.layout.Pane;
import unilim.info.ihm.tp6.exo3.tools.CardGameTools;

public class WarCardGameController {

    @FXML
    private Button idBtnChanger;

    @FXML
    private Button idBtnQuitter;

    @FXML
    private Pane idCardToBeat;

    @FXML
    private HBox idDeck;

    @FXML
    void changer(ActionEvent event) {
        //TODO
    }

    @FXML
    void quitter(ActionEvent event) {
        System.exit(0);
    }
    
    @FXML
    void initialize() {
    	Integer card1 = CardGameTools.generateCardValue();
    	Integer card2 = CardGameTools.generateCardValue();
    	Integer card3 = CardGameTools.generateCardValue();
    	Integer card4 = CardGameTools.generateCardValue();
    	Integer card5 = CardGameTools.generateCardValue();
    	Integer cardToBeat = CardGameTools.generateCardValue();
    	
    	idDeck.getChildren().add(CardGameTools.loadCardImage(card1));
    	idDeck.getChildren().add(CardGameTools.loadCardImage(card2));
    	idDeck.getChildren().add(CardGameTools.loadCardImage(card3));
    	idDeck.getChildren().add(CardGameTools.loadCardImage(card4));
    	idDeck.getChildren().add(CardGameTools.loadCardImage(card5));
    	idCardToBeat.getChildren().add(CardGameTools.loadCardImage(cardToBeat));
    	
    }
    
}