package unilim.info.ihm.tp6.exo3.controller;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.image.ImageView;
import javafx.scene.input.Dragboard;
import javafx.scene.layout.HBox;
import javafx.scene.layout.Pane;
import unilim.info.ihm.tp6.exo3.tools.CardGameTools;
import unilim.info.ihm.tp6.exo3.view.CardImageView;
import javafx.scene.input.TransferMode;


public class WarCardGameController {
	@FXML
	public ImageView imageView1;
	@FXML private ImageView imageView2;
	@FXML private ImageView imageView3;
	@FXML private ImageView imageView4;
	@FXML private ImageView imageView5;
	@FXML private ImageView cardToBeat;

    @FXML
    private Button idBtnChanger;

    @FXML
    private Button idBtnQuitter;

    @FXML
    private Pane idCardToBeat;

    @FXML
    private HBox idDeck;

    @FXML
    void changer(ActionEvent event) {
        Integer cardValue1 = CardGameTools.generateCardValue();
    	Integer cardValue2 = CardGameTools.generateCardValue();
    	Integer cardValue3 = CardGameTools.generateCardValue();	
    	Integer cardValue4 = CardGameTools.generateCardValue();
    	Integer cardValue5 = CardGameTools.generateCardValue();
    	
    	this.imageView1.setImage(CardGameTools.loadCardImage(cardValue1).getImage());
    	this.imageView2.setImage(CardGameTools.loadCardImage(cardValue2).getImage());
    	this.imageView3.setImage(CardGameTools.loadCardImage(cardValue3).getImage());
    	this.imageView4.setImage(CardGameTools.loadCardImage(cardValue4).getImage());
    	this.imageView5.setImage(CardGameTools.loadCardImage(cardValue5).getImage());
    }

    @FXML
    void quitter(ActionEvent event) {
        System.exit(0);
    }
    
    @FXML
    void initialize() {
    	changer(null);
    	Integer cardToBeat = CardGameTools.generateCardValue();
    	this.cardToBeat.setImage(CardGameTools.loadCardImage(cardToBeat).getImage());
    }
    
    public static void manageSourceDnD(CardImageView source, HBox deck) {
        CardImageView card = new CardImageView(source.getCard());

        card.setOnDragDetected(event -> {
            card.startFullDrag();
        });

        card.setOnDragOver(event -> {
            if (event.getGestureSource() != card && event.getDragboard().hasImage()) {
                event.acceptTransferModes(TransferMode.COPY);
            }
            event.consume();
        });

        card.setOnDragDropped(event -> {
            if (event.getGestureSource() != card && event.getDragboard().hasImage()) {
                deck.getChildren().add(card);
                event.setDropCompleted(true);
            } else {
                event.setDropCompleted(false);
            }
            event.consume();
        });
    }
    
    public void manageTargetDnD(CardImageView target, HBox deck) {
        target.setOnDragOver(event -> {
            if (event.getGestureSource() != target && event.getDragboard().hasImage()) {
                event.acceptTransferModes(TransferMode.COPY);
            }
            event.consume();
        });

        target.setOnDragDropped(event -> {
            if (event.getGestureSource() != target && event.getDragboard().hasImage()) {
                deck.getChildren().add(target);
                event.setDropCompleted(true);
            } else {
                event.setDropCompleted(false);
            }
            event.consume();
        });
    }

    
}